(ns testing.enemy-test
  (:require [expectations :refer :all]
            [expectations.clojure.test :refer [defexpect]]
            [play-clj.g2d :as g2d]
            [testing.enemy :as enemy]))

;;Não consegui fazer esse teste
;;(defexpect test-kotlin-entity
;;  (expect "" (enemy/create-kotlin)))
